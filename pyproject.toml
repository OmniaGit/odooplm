[build-system]
# >=77 is what added PEP 639 support: setup.py passes `license_expression`,
# which older setuptools drops with nothing but an "Unknown distribution
# option" warning — the licence would silently vanish from the metadata.
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"
